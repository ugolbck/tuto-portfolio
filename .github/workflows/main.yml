name: Build and Deploy Personal Website

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the repo
      uses: actions/checkout@v2

    - name: Log in to Docker Registry
      uses: docker/login-action@v1
      with:
        username: nologin
        password: ${{ secrets.API_KEY }}
        registry: ${{ secrets.REGISTRY_ENDPOINT }}

    - name: Build and push Docker image
      run: |
        docker build -t ${{ secrets.REGISTRY_ENDPOINT }}/personal-website:${{ github.sha }} .
        docker tag ${{ secrets.REGISTRY_ENDPOINT }}/personal-website:${{ github.sha }} ${{ secrets.REGISTRY_ENDPOINT }}/personal-website:latest
        docker push ${{ secrets.REGISTRY_ENDPOINT }}/personal-website:${{ github.sha }}
        docker push ${{ secrets.REGISTRY_ENDPOINT }}/personal-website:latest

    - name: Deploy to VPS
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SERVER_IP }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        passphrase: ${{ secrets.SSH_PASSPHRASE }}
        script: |
          docker login -u nologin -p ${{ secrets.API_KEY }} ${{ secrets.REGISTRY_ENDPOINT }}
          docker pull ${{ secrets.REGISTRY_ENDPOINT }}/personal-website:latest
          #docker stop personal-website || true
          #docker rm personal-website || true
          docker run -d --name personal-website --network host ${{ secrets.REGISTRY_ENDPOINT }}/personal-website:latest